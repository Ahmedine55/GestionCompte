@model IEnumerable<GestionCompte.Models.Operation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Compte.CompteID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.typeOperation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateOperation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.montant)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Compte.CompteID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.typeOperation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateOperation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.montant)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.OperationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OperationID })
        </td>
    </tr>
}

</table>
